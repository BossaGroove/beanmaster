nav.navbar.navbar-default.navbar-inverse(role="navigation")
	.container-fluid
		.navbar-header
			button.navbar-toggle.collapsed(type="button", data-toggle="collapse", data-target="#header_control")
				span.sr-only Toggle navigation
				span.icon-bar
				span.icon-bar
				span.icon-bar
			a.navbar-brand(href="/") Beanmaster
		.collapse.navbar-collapse#header_control
			.navbar-right.navbar-form
				if (page === 'servers')
					button.btn.btn-default#btn_search
						i.glyphicon.glyphicon-search
				if (page === 'servers' || page === 'tube')
					button.btn.btn-default#btn_toggle_auto_update
						i.glyphicon.glyphicon-pause